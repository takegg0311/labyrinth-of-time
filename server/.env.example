# STT プロバイダ。現在は openai のみ。
# 後続 Issue で faster-whisper を追加する余地を残している。
STT_PROVIDER=openai

# OpenAI の API キー（STT_PROVIDER=openai のとき必須）
OPENAI_API_KEY=sk-...

# 文字起こしに使うモデル
STT_MODEL=gpt-4o-transcribe
